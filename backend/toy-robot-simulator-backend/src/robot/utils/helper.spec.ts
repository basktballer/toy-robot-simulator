
describe('helper methods', () => {
  describe('stubs', () => {
    it('should return truthy"', () => {
      expect(true).toBeTruthy();
    });

    // it('should check each rotation and direction paring matches')
  });
})